# `check.ege.edu.ru` bruteforce

## Usage

```shell
$ python brute.py [-h] -r REGION [-t THREADS] [-c CHECKPOINT] fio
```
where:
 - fio - last name + first name + middle name (may be w/o whitespaces)
 - REGION - region of participant

Auto-saves progress if stopped